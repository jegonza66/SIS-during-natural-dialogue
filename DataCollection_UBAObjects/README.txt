To start the server, use:

java -jar UBAObjects.jar -server -dir=`pwd`/files -ini=INIFILE

where INIFILE is one of pregame.ini, game0.ini, game1.ini or game2.ini.

To start each of the two clients, use:

java -jar UBAObjects.jar -client -dir=`pwd`/files -ini=client.ini


